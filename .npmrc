# Deprecation notices from transitive dependencies are noise during a build.
# They are warnings, not errors, and none of them fail an install.
#
# Chromium download is skipped via .puppeteerrc.cjs, which is puppeteer's own
# config mechanism. Setting it here instead produces "Unknown project config"
# warnings from npm and is not the documented route.
loglevel=warn
